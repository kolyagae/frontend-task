version: "3.4"

services:
  frontend:
    image: kolyagae/frontend-task:latest
    build:
      context: .
      dockerfile: ./Dockerfile
    environment:
      NODE_ENV: production
    ports:
      - "3000:3000"
